import { TestConfig } from '../types';
export declare function initRules(testConfig: TestConfig): Promise<void>;
export declare class RulesBundleLocator {
    private cachedBundlePath;
    private userBundlePath?;
    constructor(cachedBundlePath: string, userBundlePath?: string | undefined);
    locateBundle(): string | null;
    private locateUserBundle;
    private locateCachedBundle;
}
